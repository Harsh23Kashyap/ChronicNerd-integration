const {chromium}=require('playwright-core'), assert=require('assert');
(async()=>{const browser=await chromium.launch({headless:true,executablePath:'/usr/bin/google-chrome',args:['--no-sandbox']});const page=await browser.newPage();await page.route('**/env.js',r=>r.fulfill({status:200,contentType:'text/javascript',body:"window.env={API_URL:'/api'}"}));await page.route('**/api/**',r=>{const p=new URL(r.request().url()).pathname;const body=p.endsWith('/me')?{email:'test@example.com'}:p.endsWith('/conversations')?{conversations:[{conversation_id:'old',title:'Old title'}]}:p.endsWith('/session_memory')?{entries:[{raw_question:'Old question',answer:'Claim [1].\nReferences:\n[1] A Author. Study. doi:10.1234/old',sources:[]}]}:{};return r.fulfill({status:200,contentType:'application/json',body:JSON.stringify(body)})});await page.goto('http://127.0.0.1:18187/index.html');await page.waitForSelector('.chat-message.assistant');assert.equal(await page.locator('.in-chat-source[href="https://doi.org/10.1234/old"]').count(),1);await browser.close();console.log('Legacy history DOI fallback okay')})().catch(e=>{console.error(e);process.exit(1)});
